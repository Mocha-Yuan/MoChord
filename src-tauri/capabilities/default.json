{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the ChordFlow desktop window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
